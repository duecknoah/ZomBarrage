///scr_consoleOutput(string);
// outputs a string to console, with no return space at the end
// see scr_consoleOutPrintln for a new line after

with (obj_guiController) {
    outputConsoleString = argument0 + outputConsoleString;
    showConsole = true;
}