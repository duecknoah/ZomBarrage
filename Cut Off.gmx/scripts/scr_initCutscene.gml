///scr_initCutscene();
// Inits cutscene by hiding everything from Menu except certain things

with (obj_guiController) {
    showUpgrade = false; // show upgrade gui?
    showUpgradeButton = false; // show upgrade button?
    showPause = false; // show pause gui?
    showPauseButton = false; // show pause button?
    showWeaponPickupText = false; // show weapon pickup text?
    showWaveBar = false; // show the wave bar?
    showOnFireGuiAlpha = false; // run the code to do with the on fire on the screen?
    showInventory = false; // Weapon slots and inventory
    showHealthBar = false;
}
